import { VaccinationFormErrorMessages } from './vaccination-form-error-messages';

describe('VaccinationFormErrorMessages', () => {
  it('should create an instance', () => {
    expect(new VaccinationFormErrorMessages()).toBeTruthy();
  });
});
