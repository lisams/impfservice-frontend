import { VaccinationValidators } from './vaccination-validators';

describe('VaccinationValidators', () => {
  it('should create an instance', () => {
    expect(new VaccinationValidators()).toBeTruthy();
  });
});
